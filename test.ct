type struct = {
	hello: int,
	world: bool
};
type enum = [
	hello,
	world
];
type alias = enum;
let const = 5;
let add = fn(a: int, b: int): int {
	a
};
let main = fn() {
	let handle = add(1, 2);  
	println("Hello world");  
	println(5+5);  
	if const < 3 {   
		println("math broke")  
	} else {   
		println("Its fine")  
	};  
	println(@handle); 
};
